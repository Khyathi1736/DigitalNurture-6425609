# 1. Make a small change to a file
echo "Temporary change" >> readme.md

# 2. Stage and commit it
git add readme.md
git commit -m "Temporary change for revert/reset demo"

# 3. Get the commit ID of this commit (latest one)
git log --oneline -1

# 4. Use git revert to undo this commit (makes a new commit)
git revert <commit-id>

# 5. Push the revert commit
git push origin main

# 6. (Optional) Now try git reset (dangerous — rewrites history)
# WARNING: Do not use on shared branches without coordination!
git reset --hard HEAD~1

# 7. Force push the reset branch to remote
git push origin main --force
